---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: whereabouts
  namespace: kube-system
spec:
  chartRef:
    kind: OCIRepository
    name: whereabouts
    namespace: flux-system
  interval: 12h
  values: {}
