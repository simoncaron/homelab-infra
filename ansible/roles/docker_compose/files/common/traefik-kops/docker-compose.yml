---

services:
  app:
    image: ghcr.io/jittering/traefik-kop:0.13.3
    env_file: .env
    environment:
      REDIS_ADDR: "docker01.simn.io:6379"
      DOCKER_HOST: tcp://docker-socket-proxy:2375
    restart: unless-stopped

networks:
  default:
    name: internal
    external: true
